import { colours } from './colours'

export const theme = {
    palette: {
        type: 'dark',
        secondary: {
            light: colours.RED_LIGHT,
            main: colours.RED_MAIN,
            dark: colours.RED_DARK,
            contrastText: colours.WHITE,
        },
        primary: {
            light: colours.PURPLE_LIGHT,
            main: colours.PURPLE_MAIN,
            dark: colours.PURPLE_DARK,
            contrastText: colours.WHITE,
        },
    }
}
